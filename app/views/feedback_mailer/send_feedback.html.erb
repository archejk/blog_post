<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <style>
      body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
      .container { max-width: 600px; margin: 0 auto; padding: 20px; }
      .header { background-color: #f8f9fa; padding: 20px; border-radius: 8px; margin-bottom: 20px; }
      .feedback-box { background-color: #e9ecef; padding: 15px; border-radius: 5px; margin: 15px 0; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h2>New Feedback Received!</h2>
        <p>Hello <%= @author_name %>,</p>
        <p>You have received new feedback on your blog post "<strong><%= @blog_post.title %></strong>".</p>
      </div>

      <div class="feedback-box">
        <h3>Feedback Details:</h3>
        <p><strong>From:</strong> <%= @feedback.name %> (<%= @feedback.email %>)</p>
        <p><strong>Message:</strong></p>
        <p><%= simple_format(@feedback.message) %></p>
      </div>

      <p>Thank you for using Blog Post!</p>
    </div>
  </body>
</html>